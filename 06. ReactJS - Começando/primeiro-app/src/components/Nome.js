function Nome({nome, idade}) {

  return (
    <span>Meu nome é {nome} e tenho {idade} anos.</span>
  );
}

export default Nome;

//{props.nome} ou nome}- serve para rederizar o valor da propriedade "nome" passada para o componente Nome.