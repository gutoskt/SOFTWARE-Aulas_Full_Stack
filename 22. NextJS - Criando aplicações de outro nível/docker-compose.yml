version: '3.8'

environment:
  - WATCHPACK_POLLING=true

services:
  next-app:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development